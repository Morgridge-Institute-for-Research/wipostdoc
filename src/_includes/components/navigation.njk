<header class="stickyk top-0 bg-white shadow">
	<nav class="container flex flex-col sm:flex-row justify-between items-center mx-auto py-4 px-8">
	  <a href="/" class="font-semibold text-xl tracking-tight">Wisconsin Postdoc Expo</a>

	  {% set navPages = collections.all | eleventyNavigation %}
	  <!--<ul class="flex place-items-center gap-3 flex-wrap">
		{% for entry in navPages %}
		  <li class="inline">
			<a
			  href="{{ entry.url }}"
			  class="uppercase text-sm py-1 px-2 rounded inline-block
			  {% if (entry.url in page.url and entry.url != '/') or (page.url == '/' and entry.url == '/')  %}
				bg-black text-white
			  {% else %}
				text-zinc-700 hover:bg-zinc-100
			  {% endif %}">
			  {{ entry.title }}
			</a>
		  </li>
		{% endfor %}

	  </ul>-->

	  <!--<a class="bg-blue-800 hover:bg-blue-900 text-white py-2 px-4 rounded cursor-pointer">Register Now</a>-->

	</nav>
</header>